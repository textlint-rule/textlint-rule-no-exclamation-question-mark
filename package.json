{
  "name": "textlint-rule-no-exclamation-question-mark",
  "repository": {
    "type": "git",
    "url": "https://github.com/azu/textlint-rule-no-exclamation-question-mark.git"
  },
  "author": "azu",
  "email": "azuciao@gmail.com",
  "homepage": "https://github.com/azu/textlint-rule-no-exclamation-question-mark",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/azu/textlint-rule-no-exclamation-question-mark/issues"
  },
  "files": [
    "src/",
    "lib/"
  ],
  "version": "1.0.2",
  "description": "textlint rule that disallow exclamation and question mark.",
  "main": "lib/textlint-rule-no-exclamation-question-mark.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha test/",
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build"
  },
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-jsdoc-to-assert": "^1.0.1",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.8.0",
    "mocha": "^2.4.5",
    "power-assert": "^1.3.1",
    "textlint-tester": "^1.2.0"
  },
  "dependencies": {
    "match-index": "^1.0.1",
    "textlint-rule-helper": "^1.1.5"
  }
}
